<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TellMe.Pages.LessonListPage">
    <ContentPage.Resources>
        <ResourceDictionary>

            <!--<Style x:Key="LessonList" TargetType="StackLayout">
                <Setter Property="BackgroundColor" Value="{StaticResource BackgroundGreen}"/>
                <Setter Property="Padding" Value="0,20,0,20"/>
                <Setter Property="Spacing" Value="5"/>
            </Style>

            <Style x:Key="Lesson" BasedOn="{StaticResource ListItemSegoe}" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource BackColorBisque}"/>
            </Style>-->

        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout x:Name="LessonsList">

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>